export default function HomePage() {
  return (
    <main style={{ padding: '2rem', fontFamily: 'sans-serif' }}>
      <h1>WhatsApp Webhook API System</h1>
      <p>Webhook endpoint available at <code>/api/webhook/whatsapp</code></p>
    </main>
  );
}
